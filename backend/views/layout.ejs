<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Admin Panel - Hotel Navjeevan Palace</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-900 text-white shadow-lg">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <div class="flex items-center space-x-4">
                    <i class="fas fa-hotel text-2xl"></i>
                    <h1 class="text-xl font-bold">Hotel Navjeevan Palace - Admin</h1>
                </div>
                <div class="flex items-center space-x-4">
                    <% if (typeof adminName !== 'undefined') { %>
                    <span class="text-sm">Welcome, <strong><%= adminName %></strong></span>
                    <a href="/admin/logout" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded transition">Logout</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="container mx-auto px-4 py-6">
        <%- body %>
    </div>
</body>
</html>

